// Sử dụng IIFE (Immediately Invoked Function Expression)
(function ($) {
    // var l = abp.localization.getResource('CoreFW');
    // var form = $('#EditBDocumentForm');

    // Có thể thêm logic JavaScript cụ thể cho EditModal ở đây nếu cần
    // Ví dụ: Khởi tạo datepicker, select2 cho các trường (mặc dù abp tag helper thường đã xử lý)
    // Hoặc thêm validation client-side phức tạp

    // $(function() {
    //    console.log("Edit BDocument Modal loaded.");
    //    // form.find('.datepicker').datepicker({...});
    // });

})(jQuery); // Kết thúc IIFE